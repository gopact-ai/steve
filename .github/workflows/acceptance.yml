name: acceptance
on:
  push:
    branches: [master]
  workflow_dispatch:
jobs:
  e2e-fleet:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: make e2e-fleet

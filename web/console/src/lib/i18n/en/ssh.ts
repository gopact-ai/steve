import type { sshZh } from "../zh/ssh.ts";

export const sshEn = {
    "ssh.targetAlias": "SSH alias checked",
    "ssh.checkedAddress": "Address returned by the check",
    "ssh.recheckingConnection": "Checking the registered node’s connection…",
    "ssh.recordedNodeName": "Node name in configuration",
    "ssh.recordedOwner": "Workspace recorded in configuration",
    "ssh.existing": "Existing Steve configuration found",
    "ssh.existingHint": "The check found these files or directories. This does not confirm that a service is running or which workspace it belongs to.",
    "ssh.existingPaths": "Detected locations (~ means the remote user’s home)",
    "ssh.existingUnknownPaths": "Configuration or state was detected, but this response did not include its location.",
    "ssh.existingNext": "This installation cannot be reused or merged here yet. It will be preserved, including any tasks and node identity. View this workspace’s machines and connection records first, then check again after resolving the existing installation.",
    "ssh.recheck": "Check this machine again",
    "ssh.viewMachines": "View machines in this workspace",
    "ssh.relatedHistory": "This device has connection records using the same SSH alias. These are saved plans, not proof of the same machine or installation.",
    "ssh.peerScope": "Participate in collaboration and recovery",
    "ssh.peerScopeHint": "This machine will store a collaboration copy: task content, conversations, pending questions and private memory. Only connect a machine you allow to store this data.",
    "ssh.peerScopeLogin": "Agent login credentials are not copied. Register agents separately on this machine after connecting.",
    "ssh.peerScopeAuto": "Automatic takeover eligibility starts off and can be enabled separately after connecting.",
    "ssh.peerConsent": "This draft previously allowed only {level} data. It does not yet allow the collaboration content listed above.",
    "ssh.allowPeerReview": "Allow this data and review installation",
    "ssh.executionOnly": "Connect as an execution node only",
    "ssh.executionOnlyHint": "Run assigned project tasks without storing the full collaboration copy or taking over coordination. You must install and start the node on the remote machine manually.",
    "ssh.executorLevel": "Project data this machine may process",
    "ssh.executorLevelHint": "The highest project data level this machine may receive; lower levels are also allowed. This does not copy the full collaboration record.",
    "ssh.levelPublic": "Public · e.g. open-source code and public documents",
    "ssh.levelInternal": "Internal · e.g. internal code and work documents",
    "ssh.levelRestricted": "Restricted · e.g. projects with private or sensitive information",
    "ssh.levelSealed": "Sealed · process only in environments explicitly authorized by the project",
    "ssh.resumeRegistration": "Continue checking this connection",
    "ssh.resumeHint": "Recheck connectivity using the original plan, without uploading again or overwriting the remote installation.",
    "ssh.manualExecutorHint": "Register the execution node, then run the generated startup command on that machine. This action does not install through SSH or transfer collaboration data.",

    "ssh.connect": "Connect with SSH",
    "ssh.title": "Connect a machine with SSH",
    "ssh.localAgents": "Register local agents",
    "ssh.description": "Choose a machine from SSH config, check its environment, then review the changes before connecting it.",
    "ssh.candidates": "Machines to check",
    "ssh.loading": "Reading SSH configuration…",
    "ssh.empty": "No SSH machines found",
    "ssh.emptyHint": "Add a concrete Host alias to SSH config and refresh, or use the manual Add resource action.",
    "ssh.refresh": "Refresh machines",
    "ssh.warnings": "Configuration notes",
    "ssh.via": "Connect through {proxy}",
    "ssh.proxyCommand": "Uses a proxy command from SSH config",
    "ssh.conditional": "Conditional configuration is resolved when connecting",
    "ssh.selectRequired": "Choose a machine first.",
    "ssh.check": "Check connection",
    "ssh.checking": "Checking SSH connectivity and the runtime environment…",
    "ssh.reachable": "SSH is reachable. Review how to connect this machine.",
    "ssh.notReachable": "SSH connectivity has not been confirmed.",
    "ssh.name": "Node name",
    "ssh.nameHint": "Use lowercase letters, numbers, dots, underscores or hyphens.",
    "ssh.nameInvalid": "Enter a valid node name.",
    "ssh.machineName": "Machine name",
    "ssh.machineNameHint": "The name you will recognize this machine by; spaces and any script are fine. You can rename it later in Resources.",
    "ssh.machineNameInvalid": "Enter a machine name of 1–64 characters.",
    "ssh.machineNamePlaceholder": "e.g. Office Linux box",
    "ssh.workspace": "Working directory",
    "ssh.workspaceHint": "Where this machine keeps task code and outputs; created during installation. ~ is the remote home directory.",
    "ssh.workspaceInvalid": "Enter an absolute path on the machine, or one starting with ~/.",
    "ssh.browse": "Browse…",
    "ssh.browseTitle": "Choose a working directory on the machine",
    "ssh.browseHint": "These are the folders the machine already has; browsing only reads. Open a folder to use it as is, or name a new folder inside it to be created during installation.",
 "ssh.browseExistingHint": "These are the folders the machine already has; browsing only reads. Open a folder to use it as is.",
    "ssh.browseCancel": "Hide",
    "ssh.browseUp": "Up one level",
    "ssh.browseLoading": "Reading directories on the machine…",
    "ssh.browseEmpty": "No folders inside this one.",
    "ssh.browseClimbed": "The machine has no {path} yet; this is its nearest existing parent. Name a folder below to create it during installation.",
 "ssh.browseExistingClimbed": "The machine has no {path} yet; this is its nearest existing parent.",
    "ssh.browseTruncated": "Only the first 400 folders are shown; you can type the full path above instead.",
    "ssh.browseReadOnly": "This account cannot create folders here",
    "ssh.browseNewFolder": "New folder here (optional)",
    "ssh.browseNewFolderHint": "Leave empty to use the open folder itself.",
    "ssh.browseNewFolderInvalid": "A folder name cannot contain / or control characters.",
    "ssh.browsePicked": "Will use: {path}",
    "ssh.browseUse": "Use this directory",
    "ssh.abandon": "Give up this connection and start over",
    "ssh.abandonHint": "Withdraws the registration and member record this attempt left on this side; nothing on the remote machine is changed. The machine's ~/.steve-peer still holds the cluster certificate and tokens issued for this attempt: stop the old process there and move that directory aside before checking again.",
    "ssh.abandonPlanHint": "This attempt registered nothing; giving up only discards this plan and returns to the form.",
    "ssh.abandoning": "Giving up this connection…",
    "ssh.address": "Node address",
    "ssh.addressHint": "This node reaches the machine through the SSH tunnel and does not depend on this address; other nodes use this address and port to reach it.",
    "ssh.addressRequired": "Enter the node address and port.",
    "ssh.networkSettings": "Advanced network settings",
    "ssh.networkHint": "Cluster traffic between this machine and this computer travels over the SSH session used to enroll it, so changing networks or toggling a VPN here does not matter. Other nodes still need to reach each other directly; adjust these for restricted ports.",
    "ssh.raftAddress": "Election address",
    "ssh.raftAddressHint": "Optional. Used for elections and synchronization. Defaults to the next port after the node address, such as 7702 for 7701.",
    "ssh.level": "Data level",
    "ssh.levelHint": "A full node stores collaboration records, including task content and private memory, and requires Restricted or higher. Add machines with lower data access as execution nodes using the manual Resources action.",
    "ssh.review": "Review installation",
    "ssh.planning": "Checking the environment and preparing an installation plan…",
    "ssh.changeMachine": "Choose another machine",
    "ssh.steps": "Check results",
    "ssh.plan": "Review connection changes",
    "ssh.effects": "This installation will",
    "ssh.script": "View installation script",
    "ssh.expires": "Plan valid until {time}",
    "ssh.expired": "This installation plan has expired. Check the machine and review a new plan.",
    "ssh.edit": "Edit connection details",
    "ssh.install": "Confirm installation",
    "ssh.installing": "Running this installation and waiting for the node to connect…",
    "ssh.blocked": "Resolve the issues listed in the checks first.",
    "ssh.checkInstallation": "Check this installation",
    "ssh.unconfirmed": "The installation result is unconfirmed. This plan is retained; checking uses the same plan and does not create another installation.",
    "ssh.attention": "Connection needs your attention",
    "ssh.registered": "Registered, awaiting connection",
    "ssh.unregistered": "Node registration did not complete",
    "ssh.connected": "Machine connected",
    "ssh.connectedHint": "The coordinator completed the node protocol handshake. You can view this machine in Resources.",
    "ssh.attentionHint": "Check the node state and logs using the check results. Registration is retained; the remote process may still be running.",
    "ssh.planId": "This plan",
    "ssh.history": "{count} connection records on this device",
    "ssh.historyConnected": "Connection completion recorded",
    "ssh.viewRecord": "View record",
    "ssh.connectAnother": "Connect another machine",
    "ssh.done": "Done",
    "ssh.backToResources": "Back to resources",
    "ssh.close": "Close connection window",
    "ssh.storageError": "This operation could not be saved. Enable local storage and retry.",
    "ssh.responseInvalid": "The service response was incomplete. Retry the same operation.",
    "ssh.otherInstallation": "The service returned another installation result. This plan still needs to be checked.",
    "ssh.waitingForResult": "This plan is still running. Check the same installation again shortly.",
    "ssh.progress": "Installation progress",
    "ssh.progressStep": "Step {current} of {total} · {phase}",
    "ssh.progressDone": "All {total} steps completed",
    "ssh.progressStopped": "Stopped at step {current} of {total} · {phase}",
    "ssh.phase.preflight": "Recheck environment",
    "ssh.phase.registration": "Register node",
    "ssh.phase.link": "Open SSH tunnel",
    "ssh.phase.upload": "Upload node program",
    "ssh.phase.installation": "Run the installer",
    "ssh.phase.connectivity": "Wait for the node",
    "ssh.log": "Installation log",
    "ssh.logCount": "Installation log · {count} lines",
} as const satisfies Record<keyof typeof sshZh, string>;
